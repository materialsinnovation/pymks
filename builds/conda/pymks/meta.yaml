{% set version = "0.3.3.dev17+g817dc61" %}
{% set name = "pymks" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  git_url: https://github.com/materialsinnovation/{{ name }}.git
  git_rev: 817dc61

build:
  number: 1

requirements:
  build:
    - python
    - anaconda
    - pytest
    - scikit-learn
    - pytest-cov
    - pylint
    - cython
    - sympy
    - pytables

  run:
    - python
    - anaconda
    - pytest
    - scikit-learn
    - pytest-cov
    - pylint
    - cython
    - sympy
    - pytables
    - pyparsing
    - matplotlib

about:
  home: http://{{ name }}.org
